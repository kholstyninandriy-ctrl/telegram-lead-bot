# Netlify picks up the Next.js runtime automatically, but naming it here keeps
# the build reproducible if the auto-detection ever changes.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
